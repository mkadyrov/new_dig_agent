import React from 'react';
import { View, Text } from 'react-native';
import { Icon } from 'native-base';
import { Header } from 'react-native-elements';

const Copy = () => (
  <Text style={{ width: '100%', textAlign: 'center', fontSize: 13, color: '#555', paddingTop: 20, paddingBottom: 40, }}>
    2020 Digital Agent. Все права принадлежат народу.
  </Text>
);

export default Copy;
